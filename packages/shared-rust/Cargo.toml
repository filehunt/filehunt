[package]
name = "shared-rust"
version = "0.0.1"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1.0"
aws-sdk-s3 = { version = "1.0", features = ["behavior-version-latest"] }
aws-sdk-sns = { version = "1.0", features = ["behavior-version-latest"] }
aws-sdk-sqs = { version = "1.0", features = ["behavior-version-latest"] }
aws-config = "1.0"
aws-credential-types = "1.0"
aws-types = "1.0"
bytes = "1.5"
sha2 = "0.10"
hex = "0.4"
thiserror = "1.0"
log = "0.4"
uuid = { version = "1.10", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
